import {
  setUserData,
  logout,
  CartItem,
  CartAmount,
  CallEffect,
} from '../reducers/auth/reducers';

export const authActions = {
  setUserData,
  logout,
  CartItem,
  CartAmount,
  CallEffect,
};
