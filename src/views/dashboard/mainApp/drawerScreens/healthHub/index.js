import {View, Text, SafeAreaView} from 'react-native';
import React from 'react';
import getStyles from './styles';

const HealthHubScreen = props => {
  const styles = getStyles();
  return (
    <SafeAreaView>
      <Text>styles</Text>
    </SafeAreaView>
  );
};

export default HealthHubScreen;
