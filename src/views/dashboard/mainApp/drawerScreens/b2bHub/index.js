import {View, Text, SafeAreaView} from 'react-native';
import React from 'react';
import getStyles from './styles';

const TradeHubScreen = props => {
  const styles = getStyles();
  return (
    <SafeAreaView>
      <Text>styles</Text>
    </SafeAreaView>
  );
};

export default TradeHubScreen;
